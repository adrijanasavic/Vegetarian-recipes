<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_detail_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">


        <ImageView
            android:id="@+id/detalji_image_view"
            android:layout_width="match_parent"
            android:layout_height="@dimen/layout_height_detail_image"
            android:contentDescription="@string/image_des"
            android:scaleType="centerCrop" />

        <TextView
            android:id="@+id/detaljiiii_opis"
            style="@style/DetailTextViewStyle"
            android:fontFamily="sans-serif"
            android:paddingTop="@dimen/padding"
            tools:text="Detaljan opis" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="@dimen/padding_top"
            android:paddingBottom="@dimen/padding_bottom">

        </LinearLayout>
    </LinearLayout>
</ScrollView>